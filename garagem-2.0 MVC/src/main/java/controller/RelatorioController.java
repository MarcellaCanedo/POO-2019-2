package controller;

import java.util.List;

import entity.Marca;
import model.MarcaModel;

public class RelatorioController {

	 	/*RelatorioModel relatorioModel;

	    public RelatorioController() {
	        this.relatorioModel = new RelatorioModel();
	    }

	    public Relatorio save(Relatorio relatorio){

	        return this.relatorioModel.save(relatorio);

	    }

	    public boolean update(int id, Relatorio r){
	        return relatorioModel.update(id, m);
	    }

	    public boolean delete(int id){
	        return marcaModel.delete(id);
	    }

	    public List<Marca> findAll(){
	    	return marcaModel.findAll();
	    }

	    public Marca findById(int id){
	        return marcaModel.findById(id);
	    }*/
	
}
